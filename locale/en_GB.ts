import {locale} from '../src/log.js';

export default locale({
    noTarget: 'No target for $target was defined',
    runningTargets: 'running targets: $targets',
    runningTarget: 'running target: $target',
    parsing: 'Reading from $origin',
    getRule: 'Resolve $rel to $abs',
    upToDate: '$target is up to date.',
    running: 'Running: $cmd',
    isOlder: '$selector was modified'
});